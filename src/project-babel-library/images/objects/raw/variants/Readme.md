Put images here and run `node scripts/cropImages.js`

Images like `objects\human\basictoilet\basictoilet.png` from starbound unpacked assets have variant of different color, but we only need first one.
